# Leaderboard Package

Shared ranking and standings logic.

- Model rating changes, leaderboard views, and match summary transformations here.
- Keep transport and route handlers in `apps/api` and `apps/web`.
