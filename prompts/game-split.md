# Game: The Split

You own:

- `packages/games/split/**`
- `apps/web/app/games/split/**`

Your task:

- build The Split as a multiplayer blinded-seat game with optional LLM seats
- define its state machine, room interactions, public view, telemetry, and UI

Deliver:

- game plan
- shared interface requirements
- acceptance criteria and test handoff notes

