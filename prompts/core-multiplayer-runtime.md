# Core Multiplayer + Runtime

You own:

- `apps/rooms/**`
- `packages/contracts/**`
- `packages/game-sdk/**`
- `packages/agents/**`

Your task:

- research and finalize the authoritative room protocol
- implement the blinded seat model for human and LLM seats
- define replay events and deterministic reconstruction rules
- expose game integration interfaces so game packages remain modular

Do not own:

- website pages
- user flow routes
- payments
- leaderboard
- game-specific rules beyond shared hooks

Deliver:

- detailed implementation plan
- contract changes, if any
- risk log for reconnects, identity leaks, and model latency

